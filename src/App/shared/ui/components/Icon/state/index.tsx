import {DeleteIcon, EditIcon, FilterIcon, SearchIcon} from "@shared/ui/icons";


const ICONS = {
    edit: EditIcon,
    delete: DeleteIcon,
    filter: FilterIcon,
    search: SearchIcon,
} as const

export {
    ICONS,
}