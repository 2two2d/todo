import classname from './index.module.scss'

import type { ReactElement } from 'react'

const GradientBackground = (): ReactElement => <div className={ classname['gradient-background'] } />

export default GradientBackground
