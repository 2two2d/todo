import style from './index.module.scss'

import type { ReactElement } from 'react'

const GradientBackground = (): ReactElement => <div className={ style.common } />

export default GradientBackground
